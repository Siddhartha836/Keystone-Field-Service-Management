{
  "name": "keystone-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@netlify/functions": "^2.8.2",
    "express": "^4.19.2",
    "serverless-http": "^3.2.0",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5"
  }
}
